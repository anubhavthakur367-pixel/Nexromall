'use client'
import {useEffect,useState,FormEvent} from 'react';import {useRouter} from 'next/navigation';import {supabaseBrowser} from '@/lib/supabase-browser'
export default function Wallet(){const r=useRouter();const [balance,setBalance]=useState(0);const [amount,setAmount]=useState('');const [msg,setMsg]=useState('');useEffect(()=>{(async()=>{const s=supabaseBrowser();const {data:{user}}=await s.auth.getUser();if(!user){r.replace('/login');return}const p=await s.from('profiles').select('reward_balance,balance').eq('id',user.id).single();setBalance(Number(p.data?.reward_balance??p.data?.balance??0))})()},[r]);async function submit(e:FormEvent){e.preventDefault();const n=Number(amount);if(!Number.isFinite(n)||n<=0){setMsg('Enter a valid amount.');return}const {error}=await supabaseBrowser().rpc('request_withdrawal',{p_amount:n});setMsg(error?error.message:'Withdrawal request submitted for administrator review.')}return <main className="min-h-screen bg-[#05070b] text-white px-6 py-10"><div className="mx-auto max-w-xl"><a href="/dashboard" className="text-sm text-cyan-300">← Dashboard</a><h1 className="mt-8 text-3xl font-black">Wallet</h1><div className="mt-5 rounded-3xl border border-white/10 bg-white/[.03] p-6"><p className="text-slate-400">Available rewards balance</p><p className="mt-2 text-4xl font-black">{balance}</p></div><form onSubmit={submit} className="mt-5 rounded-3xl border border-white/10 bg-white/[.03] p-6"><h2 className="font-bold">Request withdrawal</h2><p className="mt-2 text-sm text-slate-400">Requests are subject to eligibility, available balance, and administrator review.</p><input value={amount} onChange={e=>setAmount(e.target.value)} type="number" min="0" step="any" placeholder="Amount" className="mt-5 w-full rounded-xl border border-white/10 bg-black/30 p-3"/><button className="mt-4 w-full rounded-xl bg-cyan-400 p-3 font-bold text-black">Submit request</button>{msg&&<p className="mt-4 text-sm text-cyan-200">{msg}</p>}</form></div></main>}
